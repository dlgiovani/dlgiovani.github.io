DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/portfolio
FREECRYPTOAPI_KEY=your_key_here
EXCHANGERATEAPI_KEY=your_key_here
GITHUB_PAT=your_pat_here
# https://api.nasa.gov — the default DEMO_KEY is limited to ~30 req/hour/IP
# and will make /api/apod return 503 once exhausted. Get a free key there.
NASA_API_KEY=your_key_here
ALLOWED_ORIGINS=https://dlgiovani.github.io,http://localhost:4321
# secret token for reading consulting-form submissions (GET /api/consulting).
# generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
ADMIN_API_KEY=
